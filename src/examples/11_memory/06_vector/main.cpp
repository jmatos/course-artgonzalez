#include "my_vector.h"

int main() {
    // use_vector();
    // use_vector();
    // Vector v(3);
    // Vector v1 = v;

    // int num = 5;
    // int& num_Ref = num;

    //we can't do this 5 = num
    // int& five = 5;

    //We cannot access literal memory

    return 0;
}
    